// Provider transport defaults — provider only overrides fields that differ.
// See REGISTRY_TEMPLATE.js for the full registry entry contract.
export const PROVIDER_DEFAULTS = {
  format: "openai",
};
